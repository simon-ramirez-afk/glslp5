#version 330 core // Use the appropriate version for your needs

out vec4 FragColor;

void main() {
    FragColor = vec4(1.0, 1.0, 1.0, 1.0); // RGBA, all components are set to 1, resulting in white
}
